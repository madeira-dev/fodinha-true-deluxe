services:
  - type: web
    name: fodinha
    runtime: node
    plan: free
    buildCommand: npm ci --include=dev && npm run build:online
    startCommand: node dist/server.js
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: "22"
